# Secrets — copy .env.example to .env and fill in locally; never commit.
.env

# Old-gateway reference material (models.yaml, footprints, vllm.env with the real
# HF token) — kept locally for provenance, never committed.
vllm_refs/

# Local editor / OS cruft
.DS_Store
*.swp