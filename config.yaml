# llama-swap config (GENERATED by gen_config.py — do not hand-edit).
# Every single-card model is defined once per 3090: c0.<model> (3090 #0) and
# c2.<model> (3090 #2). The matrix router lets any c0 entry run alongside any c2
# entry, and a request evicts only the model on the card it needs. Models that
# need both cards are in no matrix set, so they run alone.
# Address models by card id: c0.<model> / c2.<model>, or the whole-box id.
# Regenerate: python3 gen_config.py > config.yaml
#
# HF AUTH: deliberately NOT passed as -e HF_TOKEN/-e HUGGING_FACE_HUB_TOKEN.
# llama-swap expands ${env.*} at spawn time and echoes the fully expanded
# command back from GET /running, which leaked the token in plaintext to
# anyone who could reach :9292. Instead the token is read from the HF cache
# that every model already mounts: write it to /models/hf-cache/token
# (chmod 600) on the host; huggingface_hub resolves $HF_HOME/token when no
# env var is set. Do not re-add the -e lines.

healthCheckTimeout: 900
logLevel: info
# No aliases are defined any more, so this only affects how /v1/models
# renders; left on so the listing shape does not change for consumers.
includeAliasesInList: true

models:

  # ===== c0: single-card models on GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1 =====
  "c0.gemma-26b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/gemma-4-26B-A4B-it-qat-AWQ-INT4
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 1 --gpu-memory-utilization 0.95
      --max-model-len 65536 --max-num-seqs 64
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 18000
    concurrencyLimit: 256
    useModelName: "c0.gemma-26b"
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "c0.gemma-e4b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/gemma-4-E4B-it-qat-AWQ-INT4
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 1 --gpu-memory-utilization 0.95
      --max-model-len 128000 --max-num-seqs 64
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 18000
    concurrencyLimit: 256
    useModelName: "c0.gemma-e4b"

  "c0.qwythos-v2":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1"'
      -e GGUF_REPO=empero-ai/Qwythos-9B-v2-GGUF
      -e GGUF_FILE=Qwythos-9B-v2-Q4_K_M.gguf
      -e GGUF_CTX=8192
      -e GGUF_PARALLEL=8
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/bonsai-llama:latest
      --alias ${MODEL_ID}
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 18000
    concurrencyLimit: 32

  "c0.qwen3.8-27b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1"'
      -e GGUF_REPO=unsloth/Qwen3.8-27B-GGUF
      -e GGUF_FILE=Qwen3.8-27B-UD-Q4_K_XL.gguf
      -e GGUF_CTX=16384
      -e GGUF_PARALLEL=4
      -e GGUF_MMPROJ=mmproj-F16.gguf
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_effort":"low","preserve_thinking":true}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 20
      --min-p 0.0
      --presence-penalty 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 18000
    concurrencyLimit: 16

  "c0.muse-glimmer":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1"'
      -e GGUF_REPO=meta-models/Muse-Glimmer-30B-GGUF
      -e GGUF_FILE=muse-glimmer-30B-kquant-17gb.gguf
      -e GGUF_CTX=131072
      -e GGUF_PARALLEL=1
      -e GGUF_MMPROJ=mmproj-kquant.gguf
      -e GGUF_DRAFT=dflash-kquant.gguf
      -e GGUF_SPEC_TYPE=draft-dflash
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_strength":"low"}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 64
      --min-p 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 0
    concurrencyLimit: 4

  # ===== c2: single-card models on GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb =====
  "c2.gemma-26b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/gemma-4-26B-A4B-it-qat-AWQ-INT4
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 1 --gpu-memory-utilization 0.95
      --max-model-len 65536 --max-num-seqs 64
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 18000
    concurrencyLimit: 256
    useModelName: "c2.gemma-26b"
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "c2.gemma-e4b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/gemma-4-E4B-it-qat-AWQ-INT4
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 1 --gpu-memory-utilization 0.95
      --max-model-len 128000 --max-num-seqs 64
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 18000
    concurrencyLimit: 256
    useModelName: "c2.gemma-e4b"

  "c2.qwythos-v2":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=empero-ai/Qwythos-9B-v2-GGUF
      -e GGUF_FILE=Qwythos-9B-v2-Q4_K_M.gguf
      -e GGUF_CTX=8192
      -e GGUF_PARALLEL=8
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/bonsai-llama:latest
      --alias ${MODEL_ID}
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 18000
    concurrencyLimit: 32

  "c2.qwen3.8-27b":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=unsloth/Qwen3.8-27B-GGUF
      -e GGUF_FILE=Qwen3.8-27B-UD-Q4_K_XL.gguf
      -e GGUF_CTX=16384
      -e GGUF_PARALLEL=4
      -e GGUF_MMPROJ=mmproj-F16.gguf
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_effort":"low","preserve_thinking":true}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 20
      --min-p 0.0
      --presence-penalty 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 18000
    concurrencyLimit: 16

  "c2.muse-glimmer":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=meta-models/Muse-Glimmer-30B-GGUF
      -e GGUF_FILE=muse-glimmer-30B-kquant-17gb.gguf
      -e GGUF_CTX=131072
      -e GGUF_PARALLEL=1
      -e GGUF_MMPROJ=mmproj-kquant.gguf
      -e GGUF_DRAFT=dflash-kquant.gguf
      -e GGUF_SPEC_TYPE=draft-dflash
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_strength":"low"}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 64
      --min-p 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 18000
    concurrencyLimit: 4

  # ===== Solo big models (TP=2, own both 3090s — in no matrix set) =====
  "Qwen3.6-35B-A3B-AWQ-4bit":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1,GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 2 --gpu-memory-utilization 0.9
      --max-model-len 131072 --max-num-seqs 1
      --enforce-eager
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 36000
    concurrencyLimit: 256
    useModelName: "Qwen3.6-35B-A3B-AWQ-4bit"
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "Qwen3.6-35B-A3B-32k-128seqs":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8000
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1,GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      vllm/vllm-openai:v0.26.0
      --model cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit
      --served-model-name ${MODEL_ID}
      --tensor-parallel-size 2 --gpu-memory-utilization 0.92
      --max-model-len 32768 --max-num-seqs 128
      --enforce-eager
      --limit-mm-per-prompt '{"image":0,"video":0}'
      --reasoning-parser qwen3
      --enable-auto-tool-choice
      --tool-call-parser qwen3_coder
      --enable-prefix-caching
      --mamba-cache-mode align
      --port 8000
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    ttl: 36000
    concurrencyLimit: 512
    useModelName: "Qwen3.6-35B-A3B-32k-128seqs"
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  # ===== Whole-box GGUF entries (both 3090s — in no matrix set; see UNGROUPED_GGUF) =====
  "Muse-Glimmer-30B-split":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1,GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=meta-models/Muse-Glimmer-30B-GGUF
      -e GGUF_FILE=muse-glimmer-30B-kquant-dynamic.gguf
      -e GGUF_CTX=131072
      -e GGUF_PARALLEL=1
      -e GGUF_MMPROJ=mmproj-kquant.gguf
      -e GGUF_DRAFT=dflash-kquant.gguf
      -e GGUF_SPEC_TYPE=draft-dflash
      -e GGUF_SPLIT_MODE=layer
      -e GGUF_TENSOR_SPLIT=1,1
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_strength":"low"}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 64
      --min-p 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 36000
    concurrencyLimit: 4

  "Qwen3.8-27B-split":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1,GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=unsloth/Qwen3.8-27B-GGUF
      -e GGUF_FILE=Qwen3.8-27B-UD-Q6_K_XL.gguf
      -e GGUF_CTX=262144
      -e GGUF_PARALLEL=1
      -e GGUF_MMPROJ=mmproj-F16.gguf
      -e GGUF_SPLIT_MODE=layer
      -e GGUF_TENSOR_SPLIT=1,1
      -e 'LLAMA_ARG_CHAT_TEMPLATE_KWARGS={"reasoning_effort":"low","preserve_thinking":true}'
      -v /models/hf-cache:/root/.cache/huggingface
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-mainline:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 0.95
      --top-k 20
      --min-p 0.0
      --presence-penalty 0.0
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 36000
    concurrencyLimit: 4

  "deepseek-v4-flash":
    cmd: |
      docker run --rm --name ${MODEL_ID}
      --pull=always
      --entrypoint gguf-serve.sh
      --gpus '"device=GPU-a8c640ca-4d44-440b-5caf-28eca88ea7c1,GPU-094f1ca3-2155-7b04-b5aa-4abae3b5ffeb"'
      -e GGUF_REPO=unsloth/DeepSeek-V4-Flash-0731-GGUF
      -e GGUF_FILE=UD-Q4_K_XL/DeepSeek-V4-Flash-0731-UD-Q4_K_XL-00001-of-00005.gguf
      -e GGUF_CTX=1048576
      -e GGUF_PARALLEL=1
      -e GGUF_SPLIT_MODE=layer
      -e GGUF_TENSOR_SPLIT=6,1
      -e GGUF_N_CPU_MOE=36
      -e GGUF_NUMA=distribute
      -e GGUF_THREADS=12
      -e GGUF_BATCH=4096
      -e GGUF_UBATCH=1024
      -e GGUF_DIR=/fast-gguf
      -v /models/hf-cache:/root/.cache/huggingface
      -v /fast/gguf:/fast-gguf
      -p ${PORT}:8080
      ghcr.io/tkontu/llamacpp-v4:latest
      --alias ${MODEL_ID}
      --temp 1.0
      --top-p 1.0
      --min-p 0.01
    cmdStop: docker stop ${MODEL_ID}
    proxy: http://127.0.0.1:${PORT}
    checkEndpoint: /health
    ttl: 28800
    concurrencyLimit: 4


routing:
  router:
    use: matrix
    settings:
      matrix:
        sets:
          cards: >-
            (c0.gemma-26b
              | c0.gemma-e4b
              | c0.qwythos-v2
              | c0.qwen3.8-27b
              | c0.muse-glimmer
            )
            & (c2.gemma-26b
              | c2.gemma-e4b
              | c2.qwythos-v2
              | c2.qwen3.8-27b
              | c2.muse-glimmer
            )
